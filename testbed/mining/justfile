# Runs the script
run:
    Rscript main.R

# Serves the content of the directory so graphs can be opened in a browser
serve port="9000":
    mkdir -p out/
    cd out; live-server --port={{port}}

watch:
    ls *.R | entr -nr just
